pub struct StreamChecker;

impl StreamChecker {
    pub fn new(words: Vec<String>) -> Self {
        panic!("TODO")
    }

    pub fn query(&mut self, letter: String) -> bool {
        panic!("TODO")
    }
}
