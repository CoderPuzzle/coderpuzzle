class MovieRentingSystem {
    constructor(n: number, entries: number[][]) {}

    search(movie: number): number[] {}

    rent(shop: number, movie: number) {}

    drop(shop: number, movie: number) {}

    report(): number[][] {}
}
