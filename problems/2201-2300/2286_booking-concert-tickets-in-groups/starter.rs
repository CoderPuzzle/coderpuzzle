pub struct BookMyShow;

impl BookMyShow {
    pub fn new(n: i32, m: i32) -> Self {
        panic!("TODO")
    }

    pub fn gather(&mut self, k: i32, maxRow: i32) -> Vec<i32> {
        panic!("TODO")
    }

    pub fn scatter(&mut self, k: i32, maxRow: i32) -> bool {
        panic!("TODO")
    }
}
